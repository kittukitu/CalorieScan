<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body class="flex flex-col min-h-screen text-gray-800 bg-gray-100">
     <!-- Navigation Bar -->
     <nav class="p-4 text-white bg-green-500">
        <div class="container flex items-center justify-between mx-auto">
            <h1 class="text-xl font-bold">CalorieScan</h1>
            <ul class="flex items-center space-x-4">
                <li><a href="/dashboard" class="hover:text-gray-200">Home</a></li>
                <li><a href="/profile" class="hover:text-gray-200">Profile</a></li>
                <li><a href="/about" class="hover:text-gray-200">About</a></li>
                <li><a href="/settings" class="hover:text-gray-200">Settings</a></li>
                <% if (user && user.role === 'admin') { %>
                    <li><a href="/admin/users" class="hover:text-gray-200">Admin</a></li>
                <% } %>
                
                <li>
                <li>
                    <a href="/logout" class="inline-block px-4 py-2 ml-4 transition duration-200 bg-red-500 rounded-lg hover:bg-red-600">Logout</a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container flex items-center justify-center flex-grow mx-auto mt-10">
        <div class="w-full max-w-md p-6 bg-white rounded-lg shadow">
            <h1 class="mb-4 text-2xl font-bold text-center">User Profile</h1>
            <p class="mb-2"><strong>Name:</strong> <%= user.name %></p>
            <p class="mb-2"><strong>Email:</strong> <%= user.email %></p>
            <p class="mb-2"><strong>Phone:</strong> <%= user.phone %></p>
            <p class="mb-2"><strong>Role:</strong> <%= user.role %></p>
            <p class="mb-2"><strong>Joined:</strong> <%= user.created_at %></p>

            <!-- Edit Button -->
            <div class="mt-4 text-center">
                <a href="/profile/edit" class="px-4 py-2 text-white transition duration-200 bg-blue-500 rounded-lg hover:bg-blue-600">
                    Edit Profile
                </a>
            </div>
        </div>
    </div>

    <footer class="py-4 text-white bg-green-500">
        <div class="container mx-auto text-center">
            <p>&copy; 2024 Nutrition Dashboard. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>
