<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Calorie Tracker</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>

<body class="flex flex-col min-h-screen bg-gray-100">

    <!-- Navigation Bar -->
     <!-- Navigation Bar -->
     <nav class="p-4 text-white bg-green-500">
        <div class="container flex items-center justify-between mx-auto">
            <h1 class="text-xl font-bold">CalorieScan</h1>
            <ul class="flex items-center space-x-4">
                <li><a href="/dashboard" class="hover:text-gray-200">Home</a></li>
                <li><a href="/profile" class="hover:text-gray-200">Profile</a></li>
                <li><a href="/about" class="hover:text-gray-200">About</a></li>
                <li><a href="/settings" class="hover:text-gray-200">Settings</a></li>
                <% if (user && user.role === 'admin') { %>
                    <li><a href="/admin/users" class="hover:text-gray-200">Admin</a></li>
                <% } %>
                
                <li>
                <li>
                    <a href="/logout" class="inline-block px-4 py-2 ml-4 transition duration-200 bg-red-500 rounded-lg hover:bg-red-600">Logout</a>
                </li>
            </ul>
        </div>
    </nav>
    <!-- Form Section Centered in the Page -->
    <div class="flex items-center justify-center flex-grow">
        <div class="w-full max-w-md p-6 bg-white rounded-lg shadow-lg">
            <h2 class="mb-4 text-2xl font-semibold text-center text-gray-800">Calorie Tracker</h2>
            
            <!-- Calorie Tracker Form -->
            <form action="/tracker" method="POST" class="space-y-4">
                <div>
                    <label class="block mb-2 font-bold text-gray-700" for="foodItem">Food Item</label>
                    <input type="text" name="foodItem" id="foodItem" placeholder="Food Item Name"
                        class="w-full p-3 border border-gray-300 rounded-lg" required>
                </div>
                
                <div>
                    <label class="block mb-2 font-bold text-gray-700" for="weight">Weight (g)</label>
                    <input type="number" name="weight" id="weight" placeholder="Weight in grams"
                        class="w-full p-3 border border-gray-300 rounded-lg" required>
                </div>

                <div>
                    <label class="block mb-2 font-bold text-gray-700" for="date">Date</label>
                    <input type="date" name="date" id="date"
                        class="w-full p-3 border border-gray-300 rounded-lg" required>
                </div>

                <button type="submit"
                    class="w-full py-3 text-white transition duration-200 bg-green-500 rounded-lg hover:bg-green-600">Calculate & Save</button>
            </form>
        </div>
    </div>

    <!-- Footer at the Bottom -->
    <footer class="py-4 text-white bg-green-500">
        <div class="container mx-auto text-center">
            <p>&copy; 2024 Nutrition Dashboard. All rights reserved.</p>
        </div>
    </footer>

</body>

</html>
